## 5.2 - Cài đặt DBSCAN với Python  
Viết code cài đặt DBSCAN không dùng thư viện có sẵn (như sklearn).

Dùng một dataset và so sánh kết quả với cài đặt DBSCAN của sklearn.

Lưu ý: chú thích các câu lệnh bằng tiếng Việt.